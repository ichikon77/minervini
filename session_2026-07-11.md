# 作業メモ 2026-07-11

## 今日やったこと

### 1. jpminervini_screen.py — 285A（キオクシア）がスクリーニングされない問題を修正

**原因①（根本原因）**: `get_topix_tickers()` でティッカーを `c.isdigit() and len(c) == 4` でフィルタしていたため、英数字混合コード（285Aなど）が弾かれていた。

**修正**: 正規表現 `re.match(r'^[0-9]{3}[0-9A-Z]$', c)` に変更。`import re` も追加。

> ※最初 `r'^[0-9]{4}[A-Z0-9]?$'` と書いてしまい（4桁を要求するため285Aにマッチしない）、2回目の修正で `r'^[0-9]{3}[0-9A-Z]$'`（3桁数字＋数字か英字1文字）に直して解決。

---

### 2. jpminervini_screen.py — 日経225を廃止してTOPIX全銘柄ベースのRSに統一

日経225のWikipediaパースが常に失敗してフォールバックしていたため、日経225関連コードを全削除。

**変更内容**:
- `get_nikkei225_tickers()` 関数を削除
- `get_universe()` の戻り値を `(topix, rs_universe)` のタプルから `topix` 単体に変更
- `calc_rs_rating(all_data, nikkei_tickers)` → `calc_rs_rating(all_data)` に変更（TOPIX全銘柄でスコア計算）
- `main()` 内の呼び出し箇所を対応修正
- HTMLのsubtitle表記を「日経225ベース」→「TOPIXプライム全銘柄ベース」に変更

---

### 3. haitou_screen.py — 9506（東北電力）がスクリーニングされない理由を確認

修正なし。ロジックが正しく動作していることを確認。

- 配当利回り: 3.97%（閾値3%） → OK
- 現在PBR: 0.459
- 過去2年PBR最低値: 0.410 → 閾値（×1.10）= 0.451
- 0.459 > 0.451 のため **NG**（ギリギリ不合格、意図通りの動作）

---

## 現在のスクリーナー構成

| ファイル | 対象 | 主な条件 |
|---|---|---|
| minervini_screen.py | 米国株 | ミネルヴィニ トレンドテンプレート |
| jpminervini_screen.py | TOPIXプライム | トレンドテンプレート7条件 + RS≥70（TOPIXベース）+ 営業利益率≥15% |
| haitou_screen.py | TOPIXプライム | 配当利回り≥3% + 現在PBR < 過去2年最低PBR×1.10 |

GitHub Pages: https://ichikon77.github.io/minervini/jpminervini.html

---

## 次回Claudeへの引き継ぎ事項

- 285Aのような英数字混合ティッカーはすでに対応済み（`re.match(r'^[0-9]{3}[0-9A-Z]$', c)`）
- 日経225は使っていない。RSはTOPIXプライム全銘柄ベースで計算している
- PBR_MARGIN = 0.10（変更なし）
